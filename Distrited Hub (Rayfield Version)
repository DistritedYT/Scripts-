local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "DistritedHUB V2",
   LoadingTitle = "DistritedHUB V2",
   LoadingSubtitle = "by DistritedYT",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },
   Discord = {
      Enabled = true,
      Invite = "YykSKsf2zG", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },
   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided",
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})
local Tab = Window:CreateTab("Movesets", 18434673438) -- Title, Image
local Section = Tab:CreateSection("RawPower")
local Button = Tab:CreateButton({
   Name = "RawPower Moveset",
   Callback = function()
loadstring(game:HttpGet("https://pastebin.com/raw/WeQFE7kF", true))()
   -- The function that takes place when the button is pressed
   end,
})
local Section = Tab:CreateSection("Holy Guardian")
local Button = Tab:CreateButton({
   Name = "Holy Guardian Moveset",
   Callback = function()
loadstring(game:HttpGet("https://pastebin.com/raw/B6BDbhqz", true))()
   -- The function that takes place when the button is pressed
   end,
})
local Section = Tab:CreateSection("Solar Guardian")
local Button = Tab:CreateButton({
   Name = "Solar Guardian Moveset",
   Callback = function()
loadstring(game:HttpGet("https://pastebin.com/raw/HuMMfr97", true))()
   -- The function that takes place when the button is pressed
   end,
})
local Tab = Window:CreateTab("Movesets not made by DistritedYT", 15114667107) -- Title, Image
local Section = Tab:CreateSection("ARCAURA")
local Button = Tab:CreateButton({
   Name = "Arcaura Moveset",
   Callback = function()
getgenv().OptimizeUltimate = true
--self explanatory
getgenv().ReduceUltFlash = true
--self explanatory
getgenv().Move1Insta = false
--set it to false if you dont want the first move to instant kill
getgenv().LowQualityMode = true
--set to true if you have a shit pc (like me)
getgenv().BetaConsole = true
--its a console menu so you can modify each settings whenever you want
loadstring(game:HttpGet("https://raw.githubusercontent.com/Reapvitalized/TSB/refs/heads/main/ARCAURA.lua"))()
   -- The function that takes place when the button is pressed
   end,
})
local Section = Tab:CreateSection("Volta")
local Button = Tab:CreateButton({
   Name = "Volta Moveset",
   Callback = function()
writefile("ARGOS.mp3"," ")
task.wait(2)
loadstring(game:HttpGet("https://raw.githubusercontent.com/Reapvitalized/TSB/refs/heads/main/VOLTA.lua"))()
   -- The function that takes place when the button is pressed
   end,
})
